import { FavoritesDirective } from './favorites.directive';

describe('FavoritesDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritesDirective();
    expect(directive).toBeTruthy();
  });
});
